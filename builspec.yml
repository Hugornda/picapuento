version: 0.2

phases:
  install:
    runtime-versions:
      golang: 1.18.1

  build:
    commands:
      - GOOS=linux GOARCH=amd64 go build -o main main.go
      - zip main.zip main
artifacts:
  files:
    - main.zip
